import { Routes } from '@angular/router';
import { SupplierslistComponent } from './supplierslist/supplierslist.component';

export const routes: Routes = [
    {
        path: 'suppliers', component: SupplierslistComponent, title:"Proveeores"
    }
];
